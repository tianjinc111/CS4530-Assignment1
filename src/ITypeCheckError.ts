

/**
 * interface ITpecheckError
 * defining the API for typechecker classes. 
 */
interface ITypeCheckError {

/**
 * produces a human-readable representation of the error message
 */
 toString(): string
}

export default ITypeCheckError